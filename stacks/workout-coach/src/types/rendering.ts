export interface RenderUniforms {
  time: number;
  intensity: number;
  tempoPhase: number;
  phaseColor: [number, number, number];
  phaseType: number;
  holdAnchor: number;
  screenSize: [number, number];
}
